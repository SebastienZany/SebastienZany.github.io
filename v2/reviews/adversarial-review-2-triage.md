# Adversarial review #2 (Codex, 2026-08-14) — triage and dispositions

> Round 2 ran against the round-1-amended docs (with a known mid-run edit race; no finding turned
> out to target superseded text). 25 findings. Outcome: **all 25 drove changes** — including two
> reversals of round-1 triage verdicts (rows 1 and 9 there), which is exactly what the loop is
> for. Reviewer's fresh mesh audits were independently reproduced **exactly** before acceptance
> (slit components 570 / largest 87; boundary-side adjacent-triangle altitudes < 4 texels for
> 59,215 of 60,068, median 1.49; corner angle defects +181 / −1,358 / ~flat 804 —
> `v2/tools/audit/` + `mesh-audit-results.md`).

Legend: ✅ accepted · ◐ accepted with narrower scope · (r1-N) = reverses/amends round-1 row N.

| # | Sev | Claim (compressed) | Verdict | Disposition |
|---|---|---|---|---|
| 1 | high | Exposure amendment changes legacy reserve accounting | ✅ | Right — legacy's ledger is *intentionally open*: agents credit reserves **directly** from food at their position (main.js:3210 region), while the field is depleted by the separate exposure formula (main.js:3706). My round-1 "apply uptake to both field and reserves in the resolve pass" would have changed agent dynamics. M3 now specs both sides verbatim-legacy and states the ledger is not closed (parity over conservation). |
| 2 | high | Two-ended allocator races on the crossing slot | ✅ | Correct — concurrent bottom/top counters can hand the same slot to a parent and a child. Replaced with a **two-phase step**: pass A appends survivors only (they can never exceed capacity); pass B dispatches over the written survivors, re-derives children (RNG is keyed `(parentId, step, k)`, so re-derivation is exact), and admits them with a capacity check against the now-fixed survivor count. No races, parents structurally first, keeps indirect dispatch. |
| 3 | high | Single hinge affine extrapolated beyond its defining triangle for ~every gutter row (median altitude 1.49 texels) | ✅ | The sharpest finding of the round, verified exactly. Resolution is a design upgrade: the **gutter fill table stops using affines at all** — donors are computed at bake time by an exact **geodesic mesh walk** (piecewise across as many triangles/charts as needed; CPU cost is irrelevant at bake). The hinge affine survives only where it always belonged: the *runtime agent resolver*, as the same class of single-triangle approximation legacy shipped — tested to legacy-level tolerance, not geodesic exactness. M2's transverse test now validates the walk (well-defined), and the affine test gets banded off-edge tolerances. |
| 4 | high | Round-1 row 9 (separable Gaussian rotation-invariance) is mathematically wrong | ✅ (r1-9) | Conceded — separability commutes with rotation only within one basis; across a 90° seam the H/V pair double-blurs one physical axis. Crowd blur redesigned: **iterated near-isotropic 3×3 passes** (each step's support is 1 texel, refilled between steps — rotation across seams becomes irrelevant because each step is isotropic; n steps give σ = σ₀√n, variances add — σ² math now stated correctly, fixing finding 14 too). Default radius ≈ 3.75 → ~7 cheap passes. Radii beyond σ≈3 fall back to separable H/V with a *documented* cross-seam anisotropy caveat (extreme non-default settings only). |
| 5 | high | `r32float` read_write storage is not core (needs the `readonly_and_readwrite_storage_textures` WGSL language extension) | ✅ (r1-1) | Conceded — round 1 conflated format eligibility with extension availability. PLAN wording fixed; M0 probes the *language extension* explicitly and the workload rehearsal now runs **both** fill paths (read_write and the staging-copy fallback) so a device that lacks the extension is benchmarked on the path it will actually run. |
| 6 | high | "Resolver-only" fallback for unsplittable slits breaks the invariant | ✅ | Right — display/diffusion have no resolver, so an unsplit slit is a cliff. The fallback is deleted: a boundary-reaching cut extension always exists in a connected chart, so the splitter is spec'd to **always succeed**; the suite asserts zero unsplit slits. |
| 7 | high | Splitter should target slit *components* (570, largest 87), not the 3,592 edge pairs | ✅ | Verified exactly. Splitting is per-component (one extension per curve endpoint, not per edge) — an order of magnitude less added perimeter, which also protects the packing arithmetic. Expected values updated. |
| 8 | high | Display gutters filled from the raw sim field after EMA/blur → seam ∝ temporal lag | ✅ | Real schedule bug in my §2 frame description. Display fill donors now read the **display texture itself post-EMA** (and between directional blur passes, the display data), with the same read_write/staging discipline as the sim fill. |
| 9 | high | u32 hash ids collide at planned populations (~40 expected pairs at desktop cap) | ✅ | Birthday math is right. Ids widen to **64-bit** (`id_lo`/`id_hi`, struct stays 32 B), children take `hash64(parentId64, step, k)` — collision probability negligible; survivor-by-id and RNG-stream uniqueness become sound. |
| 10 | high | Memory table mis-added again; display branch omitted; "less memory than legacy" false | ✅ | Conceded — and the table is now **computed by a checked-in script**, not by hand: 212–275 MB at 1536 (maps f16/f32 × display r16f/rgba16f branches) + 30–90 MB canvas-dependent depth/swapchain; 112–140 MB at 1024. Claim reworded: desktop is *comparable* to legacy's 306 MiB while carrying 4× the texels; mobile is ~half of legacy's at 1.8× the texels. |
| 11 | high | MIN_CHART=4 with 4-texel donors can't keep footprints authoritative on 4-wide charts | ✅ | Subsumed by finding 3's fix: geodesic-walk donors are multi-hop by construction, so a continuation crossing a narrow chart lands wherever the surface actually continues. Noted explicitly in M2. |
| 12 | high | Slit-split output can't be represented by one `uv1` per original vertex | ✅ | Right — cut vertices need duplication. The bake now emits its **own reindexed geometry** (duplicated vertices along cuts, per-baked-vertex `uv1`); M1's `MESH1` keeps original geometry, M2's atlas sections carry the derived geometry the renderer/sim actually use. |
| 13 | med | Corner "geodesic reference" undefined at cone points (defects: +181 / −1,358) | ◐ | Verified. With walk-based donors each gutter texel's donor is *constructively* defined (walk across its nearest boundary), including at defect vertices — but no method can be C1 through a cone point. Spec now says exactly that: corners are C0 with bounded gradient error inside a ≤2-texel disc around defect vertices (measured, reported, artist-judged — 1,539 defect corners on the whole mesh, localized); the corner test checks per-edge-local consistency, not a global reference. |
| 14 | med | Gaussian variance/radius math and profile parity unstated | ✅ | Folded into finding 4's redesign (σ² addition stated; target = match legacy disc's RMS width; only-default-tested gap fixed by testing the slider's ends too). |
| 15 | med | "Conservative" single-hop claim is false (legacy accepts endpoint-only chart match — chords can tunnel); promised incidence metric missing | ✅ | Right on legacy semantics (main.js:2200/2258 check only the endpoint). Wording fixed — v2 keeps *endpoint-semantics parity*, tunneling included; M2's report gains the multi-hop/sensor-crossing incidence metric that PLAN promised. |
| 16 | med | Mass-drift test can't see wrong diffusion speed/anisotropy across rescaled charts | ✅ | Added an **impulse-spread test**: delta injections near seams (fixtures + sampled real charts), world-space second moment after n steps vs seamless reference, bounded and reported; per-chart diffusion compensation noted as an optional lever alongside the step/sense one. |
| 17 | med | Controller amendment keeps a stale contract line + wrong anchors | ✅ | Anchors corrected (supply update ~main.js:19455; secondary actuator ~19303); "suppress-only never-extinct" replaced by the actual law (log-domain supply feedback, bounded by supply floor/ceiling = anti-windup); added a **controller-law test** (log-supply step response to growth error), since the outcome-only target test can't distinguish the wrong law. |
| 18 | med | No tests pin oat max-combine / separation / rationing / 64-oat capacity | ✅ | Four explicit M3 tests added (max not sum on overlap; oat contribution vanishes when oats clear — proves non-persistence; rationing on/off changes sensed food as specced; 64 oats honored — prototype's 8 is named as wrong). |
| 19 | med | Hardware-adapter gate should use `isFallbackAdapter`, not string parsing | ✅ | Fixture and probe now use the API flag as primary signal (strings logged as secondary diagnostics only). |
| 20 | med | Round-1 row 28's "no-mips safeguards" were never written into the docs | ✅ (r1-28) | Fair catch of triage-vs-docs drift. PLAN §2 rules now carry the explicit prohibition (never generate mips on atlas textures; display chain is the AA mechanism; distant-camera artist check at M5; chart-aware mips = post-M10 note). |
| 21 | med | GPU gutter-fill kernel has an owner but no implementing package | ✅ | New package **AK (atlas GPU kernels)** in §4.1: C → AK → {E, F4}; small, Track A-owned, briefed at C's completion. M2's GPU prohibition stands; AK is where the WGSL twin of the CPU oracle lives. |
| 22 | med | Params contract can't be produced from `main.js:325–546` alone (ranges live in index.html; defaults reference outside consts) | ✅ | Extraction sources widened: main.js params/presets **+ index.html slider min/max/step + the referenced constants**, with the binding table (main.js:18212–18253) as the completeness cross-check. |
| 23 | med | Per-section files aren't bound to one bake (cache mixing) | ✅ | Added a **manifest**: bake UUID + per-file content hashes, files content-addressed (`…<hash8>.bin.gz`); the loader fetches the manifest first and refuses mixed bakes by construction. |
| 24 | med | No 1024 look gate | ✅ | M5 gains an explicit 1024 inspection (desktop `?field=1024`), M9 the on-device one. |
| 25 | low | "Start-anytime" lanes vs briefs-required process contradiction | ✅ | §4.1/§5 wording: a lane starts when its brief exists; Claude cuts lane briefs on demand at kickoff — just-in-time applies to lane starts, not only milestone boundaries. |

## Also in this batch (from my own audit, not the review)

- **1024 packing feasibility**: my measured per-chart dilated demand at GUTTER=4 is 117.3% of
  768² (impossible — confirmed), **99.2% of 1024²** (impossible as-specced), 82.4% of 1536²
  (mask packing ≥ ~85% efficiency required; bbox packing is out). M2 gains the levers: mobile
  density dial (~10% linear) and/or mobile GUTTER=3 with CT-tiered bump taps. Component-based
  slit splitting (finding 7) protects these numbers.
- The audit scripts and full numbers are checked in under `v2/tools/audit/` — they double as the
  seed of M1's segmentation/pairing implementation.
